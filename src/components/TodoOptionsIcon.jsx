import { HamburgerIcon } from "@chakra-ui/icons";
import { IconButton } from "@chakra-ui/react";

const TodoOptionsIcon = () => {
  return <IconButton aria-label="Todo Options" icon={<HamburgerIcon />} />;
};

export default TodoOptionsIcon;
